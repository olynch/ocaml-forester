(executables
 (names main)
 (preprocess
  (pps ppx_repr ppx_deriving.show))
 (libraries brr note note.brr iri forester.core forester.parser asai)
 (modes js))

(rule
 (targets main.js)
 (deps main.bc.js)
 (action
  (run cp %{deps} %{targets})))

(alias
 (name app)
 (deps main.js index.html style.css))

;; needed because of Xmlns_effect

(env
 (_
  (js_of_ocaml
   (flags :standard --enable=effects))))
