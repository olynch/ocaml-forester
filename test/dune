;;; SPDX-FileCopyrightText: 2024 The Forester Project Contributors
;;;
;;; SPDX-License-Identifier: GPL-3.0-or-later

(env
 (dev
  (flags (:standard -w -66-32-26)))
 (_
  (env-vars
   (FORESTERLSP_TEST true))))

(cram
  (deps
    %{bin:forester}
    (glob_files_rec ./forest*)
    ))

(tests
 (names parse DSL Expansion Import_graph Output Transclusion Errors E2e)
 (preprocess
  (pps ppx_deriving.show ppx_yojson_conv))
 (deps
   (glob_files_rec ./trees/* )
   (glob_files_rec ./imports/*))
 (libraries
  alcotest
  fmt
  forester.frontend
  forester.prelude
  forester.compiler
  forester.core
  forester.forest
  pure-html
  yuujinchou
  logs
  iri
  eio
  eio.core
  eio.unix
  eio_main
  asai
  lsp
  yojson
  jsonrpc))

